[project]
name = "aws-cli-auth"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.2",
    "langchain-aws>=1.5.0",
    "python-dotenv>=1.2.2",
]
